# Документация API

## Базовый URL
```
https://transdigital.pythonanywhere.com
```

## Общая информация

API предоставляет интерфейс для управления компаниями, пользователями, транспортными средствами, маршрутами и отчетами. Все запросы, кроме GET, используют метод POST и принимают данные в формате JSON.

---

## 📋 Содержание

1. [Компании](#-компании)
2. [Пользователи](#-пользователи)
3. [Транспортные средства](#-транспортные-средства)
4. [Маршруты](#-маршруты)
5. [Отчеты](#-отчеты)
6. [Коды ошибок](#-коды-ошибок)
7. [Примеры](#-примеры)

---

## 🏢 Компании

### 1. Добавить компанию
**Эндпоинт:** `POST /api/add_company`

**Тело запроса (JSON):**
```json
{
    "company": "название_компании"
}
```

**Примечание:** Название компании не должно содержать пробелов.

**Ответ:**
```json
{
    "status": "ok",
    "message": "company added"
}
```
или
```json
{
    "status": "error",
    "message": "описание ошибки"
}
```

### 2. Удалить компанию
**Эндпоинт:** `POST /api/delete_company`

**Тело запроса (JSON):**
```json
{
    "company": "название_компании"
}
```

**Ответ:**
```json
{
    "status": "ok",
    "message": "company deleted"
}
```
или
```json
{
    "status": "error",
    "message": "описание ошибки"
}
```

---

## 👥 Пользователи

### 3. Добавить пользователя
**Эндпоинт:** `POST /api/add_user/<company>`

**Параметры:**
- `company` (string) - название компании (например, "rotor")

**Тело запроса (JSON):**
```json
{
    "name": "Иван Иванов",
    "password": "пароль123",
    "post": "Менеджер",
    "account": "ivanov_i",
    "vk": "vk.com/ivanov",
    "disciplinary_actions": "0",
    "note": "Пример заметки"
}
```

**Примечание:** Поля `disciplinary_actions` и `note` являются необязательными. По умолчанию:
- `disciplinary_actions`: "0"
- `note`: "-"

**Ответ:**
```json
{
    "status": "ok",
    "message": "user added"
}
```

### 4. Обновить пользователя
**Эндпоинт:** `POST /api/update_user/<company>`

**Параметры:**
- `company` (string) - название компании

**Тело запроса (JSON):**
```json
{
    "name": "Иван Иванов",
    "password": "новый_пароль456",
    "post": "Старший менеджер",
    "account": "ivanov_i",
    "vk": "vk.com/ivanov_new",
    "disciplinary_actions": "1",
    "note": "Обновленная заметка"
}
```

**Примечание:** Поле `note` является необязательным. По умолчанию: "-"

**Ответ:**
```json
{
    "status": "ok",
    "message": "user updated"
}
```

### 5. Удалить пользователя
**Эндпоинт:** `POST /api/delete_user/<company>`

**Параметры:**
- `company` (string) - название компании

**Тело запроса (JSON):**
```json
{
    "name": "Иван Иванов"
}
```

**Ответ:**
```json
{
    "status": "ok",
    "message": "user deleted"
}
```

### 6. Получить базовую информацию о пользователе
**Эндпоинт:** `POST /api/get_user/<company>`

**Параметры:**
- `company` (string) - название компании

**Тело запроса (JSON):**
```json
{
    "name": "Иван Иванов"
}
```

**Ответ:**
```json
{
    "status": "ok",
    "name": "Иван Иванов",
    "password": "пароль123"
}
```

### 7. Получить расширенную информацию о пользователе
**Эндпоинт:** `POST /api/get_user_info/<company>`

**Параметры:**
- `company` (string) - название компании

**Тело запроса (JSON):**
```json
{
    "name": "Иван Иванов"
}
```

**Ответ:**
```json
{
    "status": "ok",
    "name": "Иван Иванов",
    "post": "Менеджер",
    "account": "ivanov_i",
    "vk": "vk.com/ivanov",
    "disciplinary_actions": "0",
    "note": "Пример заметки"
}
```

### 8. Получить информацию обо всех пользователях компании
**Эндпоинт:** `GET /api/get_users_info/<company>`

**Параметры:**
- `company` (string) - название компании

**Тело запроса:** не требуется

**Ответ:**
```json
{
    "status": "ok",
    "users": [
        {
            "name": "Иван Иванов",
            "post": "Менеджер",
            "account": "ivanov_i",
            "vk": "vk.com/ivanov",
            "disciplinary_actions": "0",
            "note": "Пример заметки"
        }
    ]
}
```

---

## 🚗 Транспортные средства

### 9. Добавить транспортное средство
**Эндпоинт:** `POST /api/add_vehicle/<company>`

**Параметры:**
- `company` (string) - название компании

**Тело запроса (JSON):**
```json
{
    "board_number": "B-1234",
    "state_number": "A123BC777",
    "model": "Toyota Camry",
    "built": "2020",
    "since": "2021-01-15",
    "note": "Компанейский автомобиль",
    "state": "active",
    "owner": "Иван Иванов"
}
```

**Примечание:** Все поля, кроме `board_number`, являются необязательными и могут быть пустыми строками.

**Ответ:**
```json
{
    "status": "ok",
    "message": "vehicle added"
}
```

### 10. Получить все транспортные средства компании
**Эндпоинт:** `GET /api/get_vehicles/<company>`

**Параметры:**
- `company` (string) - название компании

**Тело запроса:** не требуется

**Ответ:**
```json
{
    "status": "ok",
    "vehicles": [
        {
            "number": "1",
            "board_number": "B-1234",
            "state_number": "A123BC777",
            "model": "Toyota Camry",
            "built": "2020",
            "since": "2021-01-15",
            "note": "Компанейский автомобиль",
            "state": "active",
            "owner": "Иван Иванов"
        }
    ]
}
```

### 11. Обновить транспортное средство
**Эндпоинт:** `POST /api/update_vehicle/<company>`

**Параметры:**
- `company` (string) - название компании

**Тело запроса (JSON):**
```json
{
    "number": "1",
    "board_number": "B-1234",
    "state_number": "A123BC777",
    "model": "Toyota Camry",
    "built": "2020",
    "since": "2021-01-15",
    "note": "Обновленная заметка",
    "state": "inactive",
    "owner": "Петр Петров"
}
```

**Примечание:** Все поля, кроме `number`, являются необязательными. Если поле не указано, оно останется без изменений.

**Ответ:**
```json
{
    "status": "ok",
    "message": "vehicle updated"
}
```

### 12. Удалить транспортное средство
**Эндпоинт:** `POST /api/delete_vehicle/<company>`

**Параметры:**
- `company` (string) - название компании

**Тело запроса (JSON):**
```json
{
    "number": "1"
}
```

**Ответ:**
```json
{
    "status": "ok",
    "message": "vehicle deleted"
}
```

---

## 🗺️ Маршруты

### 13. Получить все маршруты компании
**Эндпоинт:** `GET /api/get_routes/<company>`

**Параметры:**
- `company` (string) - название компании

**Тело запроса:** не требуется

**Ответ:**
```json
{
    "status": "ok",
    "routes": ["Маршрут 1", "Маршрут 2", "Маршрут 3"]
}
```

### 14. Добавить маршрут
**Эндпоинт:** `POST /api/add_route/<company>`

**Параметры:**
- `company` (string) - название компании

**Тело запроса (JSON):**
```json
{
    "route": "Новый маршрут"
}
```

**Ответ:**
```json
{
    "status": "ok",
    "message": "route added"
}
```

### 15. Удалить маршрут
**Эндпоинт:** `POST /api/delete_route/<company>`

**Параметры:**
- `company` (string) - название компании

**Тело запроса (JSON):**
```json
{
    "route": "Маршрут 1"
}
```

**Ответ:**
```json
{
    "status": "ok",
    "message": "route deleted"
}
```

---

## 📊 Отчеты

### 16. Получить все отчеты компании
**Эндпоинт:** `GET /api/get_reports/<company>`

**Параметры:**
- `company` (string) - название компании

**Тело запроса:** не требуется

**Ответ:**
```json
{
    "status": "ok",
    "reports": [
        {
            "id": "1",
            "user_name": "Иван Иванов",
            "date": "2024-01-15",
            "route": "Маршрут 1",
            "num_round_trips": "5",
            "num_passengers": "50",
            "proof": "фото_доказательство.jpg",
            "status": "not verified"
        }
    ]
}
```

**Статусы отчетов:**
- `not verified` - не проверен
- `verified` - подтвержден
- `rejected` - отклонен

### 17. Добавить отчет
**Эндпоинт:** `POST /api/add_report/<company>`

**Параметры:**
- `company` (string) - название компании

**Тело запроса (JSON):**
```json
{
    "user_name": "Иван Иванов",
    "date": "2024-01-15",
    "route": "Маршрут 1",
    "num_round_trip": "5",
    "num_passengers": "50",
    "proof": "фото_доказательство.jpg"
}
```

**Ответ:**
```json
{
    "status": "ok",
    "message": "report added"
}
```

### 18. Удалить отчет
**Эндпоинт:** `POST /api/delete_report/<company>`

**Параметры:**
- `company` (string) - название компании

**Тело запроса (JSON):**
```json
{
    "id": "1"
}
```

**Ответ:**
```json
{
    "status": "ok",
    "message": "report deleted"
}
```

### 19. Подтвердить отчет
**Эндпоинт:** `POST /api/verify_report/<company>`

**Параметры:**
- `company` (string) - название компании

**Тело запроса (JSON):**
```json
{
    "id": "1"
}
```

**Ответ:**
```json
{
    "status": "ok",
    "message": "report verified"
}
```

### 20. Отклонить отчет
**Эндпоинт:** `POST /api/reject_report/<company>`

**Параметры:**
- `company` (string) - название компании

**Тело запроса (JSON):**
```json
{
    "id": "1"
}
```

**Ответ:**
```json
{
    "status": "ok",
    "message": "report rejected"
}
```

---

## 🚨 Коды ошибок

- `company name is empty` - название компании пустое
- `company already exists` - компания уже существует
- `company does not exist` - компания не существует
- `company name cannot contain spaces` - название компании не должно содержать пробелов
- `some fields are empty` - не все обязательные поля заполнены
- `name or password is empty` - имя или пароль пустые
- `user already exists` - пользователь уже существует
- `user does not exist` - пользователь не существует
- `no users found` - пользователи не найдены
- `board_number is empty` - бортовой номер пустой
- `vehicle already exists` - транспортное средство уже существует
- `vehicle does not exist` - транспортное средство не существует
- `no vehicles found` - транспортные средства не найдены
- `route is empty` - маршрут пустой
- `route already exists` - маршрут уже существует
- `route does not exist` - маршрут не существует
- `no routes found` - маршруты не найдены
- `id is empty` - ID пустой
- `no reports found` - отчеты не найдены

---

## 💡 Примеры использования (JavaScript)

```javascript
// Добавить пользователя
const addUser = async () => {
    const response = await fetch('https://transdigital.pythonanywhere.com/api/add_user/rotor', {
        method: 'POST',
        headers: {
            'Content-Type': 'application/json',
        },
        body: JSON.stringify({
            name: 'Иван Иванов',
            password: 'пароль123',
            post: 'Менеджер',
            account: 'ivanov_i',
            vk: 'vk.com/ivanov'
        })
    });
    const result = await response.json();
    console.log(result);
};

// Получить всех пользователей
const getUsers = async () => {
    const response = await fetch('https://transdigital.pythonanywhere.com/api/get_users_info/rotor', {
        method: 'GET'
    });
    const result = await response.json();
    console.log(result);
};

// Добавить транспортное средство
const addVehicle = async () => {
    const response = await fetch('https://transdigital.pythonanywhere.com/api/add_vehicle/rotor', {
        method: 'POST',
        headers: {
            'Content-Type': 'application/json',
        },
        body: JSON.stringify({
            board_number: 'B-1234',
            model: 'Toyota Camry',
            owner: 'Иван Иванов'
        })
    });
    const result = await response.json();
    console.log(result);
};

// Добавить отчет
const addReport = async () => {
    const response = await fetch('https://transdigital.pythonanywhere.com/api/add_report/rotor', {
        method: 'POST',
        headers: {
            'Content-Type': 'application/json',
        },
        body: JSON.stringify({
            user_name: 'Иван Иванов',
            date: '2024-01-15',
            route: 'Маршрут 1',
            num_round_trip: '5',
            num_passengers: '50',
            proof: 'photo.jpg'
        })
    });
    const result = await response.json();
    console.log(result);
};
```

---

## 📝 Важные примечания

1. Все запросы к эндпоинтам с `<company>` требуют указания существующего названия компании
2. Названия компаний должны быть в нижнем регистре и без пробелов
3. Все данные передаются в кодировке UTF-8
4. CORS настроен для всех доменов
5. Поля `proof` в отчетах могут содержать ссылки на изображения или другие доказательства
6. Статусы отчетов: `not verified` (по умолчанию), `verified`, `rejected`
7. Поле `disciplinary_actions` по умолчанию "0" (можно использовать для подсчета взысканий)
8. Поле `note` по умолчанию "-" для дополнительных заметок
