# Документация API для фронтенда

## Базовый URL
```
https://rotorbus.ru
```

## Общая информация
API построено на REST архитектуре. Все запросы должны содержать заголовок:
```
Content-Type: application/json
```

Все успешные ответы содержат поле `"status": "ok"`, ошибки содержат `"status": "error"` и поле `"message"` с описанием ошибки.

## Аутентификация
API не требует аутентификации для большинства операций. В будущем может быть добавлена система токенов.

---

## 📊 Основные сущности

### 1. Компании
Каждая компания имеет свою собственную базу данных. Все операции выполняются в контексте конкретной компании.

### 2. Пользователи
Пользователи в системе с различными должностями (водитель, кондуктор, администратор).

### 3. Маршруты
Транспортные маршруты с фиксированной оплатой за рейс.

### 4. Отчеты
Ежедневные отчеты о работе (рейсы и пассажиры).

### 5. Транспортные средства
Автобусы/транспорт с технической информацией.

### 6. Коэффициенты
Коэффициенты для расчета зарплаты по должностям.

### 7. Статистика
Сводная статистика по пользователям.

---

## 📋 API Endpoints

### Компании

#### Получить список всех компаний
```http
GET /api/companies
```

**Ответ:**
```json
{
  "status": "ok",
  "companies": ["company1", "company2", "company3"]
}
```

#### Добавить новую компанию
```http
POST /api/company
```

**Тело запроса:**
```json
{
  "company": "название компании"
}
```

**Ответ:**
```json
{
  "status": "ok",
  "message": "company added"
}
```

#### Удалить компанию
```http
DELETE /api/company/{company_name}
```

---

### Пользователи

#### Получить всех пользователей компании
```http
GET /api/users/{company_name}
```

**Ответ:**
```json
{
  "status": "ok",
  "users": [
    {
      "id": 1,
      "name": "Иван Иванов",
      "post": "водитель",
      "account": "41001234567890",
      "vk": "id123456",
      "disciplinary_actions": "0",
      "note": "стаж 5 лет"
    }
  ]
}
```

#### Добавить пользователя
```http
POST /api/user/{company_name}
```

**Тело запроса:**
```json
{
  "name": "Иван Иванов",
  "password": "пароль123",
  "post": "водитель",
  "account": "41001234567890",
  "vk": "id123456",
  "disciplinary_actions": "0",
  "note": "стаж 5 лет"
}
```

**Обязательные поля:** `name`, `password`, `post`

#### Получить информацию о пользователе
```http
GET /api/user/{company_name}/{user_id}
```

#### Обновить информацию о пользователе
```http
PUT /api/user/{company_name}/{user_id}
```

**Тело запроса:** JSON с полями для обновления.

**НЕЛЬЗЯ обновлять:** `id`, `created_at`

#### Удалить пользователя
```http
DELETE /api/user/{company_name}/{user_id}
```

---

### Маршруты

#### Получить все маршруты
```http
GET /api/routes/{company_name}
```

**Ответ:**
```json
{
  "status": "ok",
  "routes": [
    {
      "id": 1,
      "route": "Маршрут №1",
      "salary": 500.0,
      "created_at": "2024-01-15 10:30:00"
    }
  ]
}
```

#### Добавить маршрут
```http
POST /api/route/{company_name}
```

**Тело запроса:**
```json
{
  "route": "Маршрут №1",
  "salary": 500.0
}
```

**Обязательные поля:** `route`, `salary`

#### Получить информацию о маршруте
```http
GET /api/route/{company_name}/{route_id}
```

#### Обновить информацию о маршруте
```http
PUT /api/route/{company_name}/{route_id}
```

#### Удалить маршрут
```http
DELETE /api/route/{company_name}/{route_id}
```

#### Получить зарплату для маршрута
```http
POST /api/route/salary/{company_name}
```

**Тело запроса:**
```json
{
  "route": "Маршрут №1"
}
```

---

### Отчеты

#### Получить все отчеты
```http
GET /api/reports/{company_name}
```

**Параметры запроса (опционально):**
- `user_name` - фильтр по пользователю
- `status` - фильтр по статусу (`not_verified`, `verified`, `rejected`)

**Пример:**
```http
GET /api/reports/company1?user_name=Иван&status=not_verified
```

#### Добавить отчет
```http
POST /api/report/{company_name}
```

**Тело запроса:**
```json
{
  "user_name": "Иван Иванов",
  "date": "2024-01-15",
  "route": "Маршрут №1",
  "num_round_trips": 5,
  "num_passengers": 120,
  "proof": "ссылка на фото"
}
```

**Обязательные поля:** `user_name`, `date`, `route`, `num_round_trips`, `num_passengers`

#### Получить информацию об отчете
```http
GET /api/report/{company_name}/{report_id}
```

#### Обновить отчет
```http
PUT /api/report/{company_name}/{report_id}
```

#### Удалить отчет
```http
DELETE /api/report/{company_name}/{report_id}
```

#### Верифицировать отчет
```http
POST /api/report/verify/{company_name}/{report_id}
```

**При верификации:** автоматически рассчитывается и начисляется зарплата пользователю.

#### Отклонить отчет
```http
POST /api/report/reject/{company_name}/{report_id}
```

---

### Транспортные средства

#### Получить все транспортные средства
```http
GET /api/vehicles/{company_name}
```

**Ответ:**
```json
{
  "status": "ok",
  "vehicles": [
    {
      "number": 1,
      "board_number": "А123АА123",
      "state_number": "1234",
      "model": "ПАЗ-3205",
      "built": "2020",
      "since": "2021-01-15",
      "note": "Замена двигателя в 2023",
      "state": "active",
      "owner": "ИП Петров",
      "created_at": "2024-01-15 10:30:00"
    }
  ]
}
```

#### Добавить транспортное средство
```http
POST /api/vehicle/{company_name}
```

**Тело запроса:**
```json
{
  "board_number": "А123АА123",
  "state_number": "1234",
  "model": "ПАЗ-3205",
  "built": "2020",
  "since": "2021-01-15",
  "note": "Замена двигателя в 2023",
  "state": "active",
  "owner": "ИП Петров"
}
```

**Обязательные поля:** `board_number`

#### Получить информацию о транспортном средстве
```http
GET /api/vehicle/{company_name}/{vehicle_id}
```

#### Обновить информацию о транспортном средстве
```http
PUT /api/vehicle/{company_name}/{vehicle_id}
```

#### Удалить транспортное средство
```http
DELETE /api/vehicle/{company_name}/{vehicle_id}
```

---

### Коэффициенты

#### Получить все коэффициенты
```http
GET /api/coefs/{company_name}
```

**Ответ:**
```json
{
  "status": "ok",
  "coefs": [
    {
      "id": 1,
      "post": "водитель",
      "coef": 1.0,
      "created_at": "2024-01-15 10:30:00"
    },
    {
      "id": 2,
      "post": "кондуктор",
      "coef": 0.8,
      "created_at": "2024-01-15 10:30:00"
    }
  ]
}
```

#### Добавить коэффициент
```http
POST /api/coef/{company_name}
```

**Тело запроса:**
```json
{
  "post": "механик",
  "coef": 0.9
}
```

**Обязательные поля:** `post`, `coef`

#### Обновить коэффициент
```http
PUT /api/coef/{company_name}/{coef_id}
```

#### Удалить коэффициент
```http
DELETE /api/coef/{company_name}/{coef_id}
```

---

### Статистика

#### Получить всю статистику
```http
GET /api/statistics/{company_name}
```

**Ответ:**
```json
{
  "status": "ok",
  "statistics": [
    {
      "id": 1,
      "user_name": "Иван Иванов",
      "post": "водитель",
      "total_salary": 150000.0,
      "period_salary": 25000.0,
      "total_round_trips": 300,
      "period_round_trips": 50,
      "total_passengers": 7200,
      "period_passengers": 1200
    }
  ]
}
```

#### Получить статистику пользователя
```http
GET /api/statistics/{company_name}/{user_name}
```

#### Сбросить периодическую статистику
```http
POST /api/statistics/{company_name}/reset
```

**Сбрасывает:** `period_salary`, `period_round_trips`, `period_passengers` у всех пользователей.

---

### Конфигурация

#### Получить все конфигурации
```http
GET /api/config/{company_name}
```

**Ответ:**
```json
{
  "status": "ok",
  "configs": {
    "passanger_cost": "2000"
  }
}
```

#### Обновить конфигурацию
```http
POST /api/config/{company_name}
```

**Тело запроса:**
```json
{
  "key": "passanger_cost",
  "value": "2500"
}
```

---

## 🎯 HTML страницы

#### Дашборд пользователя
```
https://rotorbus.ru/dashboard/{username}
```

#### Страница со списком пользователей
```
https://rotorbus.ru/users
```

#### Главная страница
```
https://rotorbus.ru/
```

---

## ⚠️ Особенности и ограничения

1. **Имена компаний** не должны содержать пробелов
2. **При верификации отчета** автоматически рассчитывается зарплата:
   ```
   Зарплата = (рейсы × оплата_за_рейс + пассажиры × стоимость_пассажира) × коэффициент_должности
   ```
3. **Статистика** обновляется только при верификации отчетов
4. **Коэффициенты по умолчанию:**
   - Водитель: 1.0
   - Кондуктор: 0.8
   - Администратор: 1.2
5. **Стоимость пассажира по умолчанию:** 2000

---

## 📝 Примеры использования

### Пример 1: Создание отчета
```javascript
fetch('https://rotorbus.ru/api/report/company1', {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json',
  },
  body: JSON.stringify({
    user_name: 'Иван Иванов',
    date: '2024-01-15',
    route: 'Маршрут №1',
    num_round_trips: 5,
    num_passengers: 120,
    proof: 'https://example.com/photo.jpg'
  })
})
.then(response => response.json())
.then(data => console.log(data));
```

### Пример 2: Получение пользователей
```javascript
fetch('https://rotorbus.ru/api/users/company1')
  .then(response => response.json())
  .then(data => {
    if (data.status === 'ok') {
      data.users.forEach(user => {
        console.log(`${user.name} - ${user.post}`);
      });
    }
  });
```

### Пример 3: Верификация отчета
```javascript
fetch('https://rotorbus.ru/api/report/verify/company1/123', {
  method: 'POST'
})
.then(response => response.json())
.then(data => {
  if (data.status === 'ok') {
    console.log(`Начислено зарплаты: ${data.calculated_salary}`);
  }
});
```

---

## 🚨 Коды ошибок

Все ошибки возвращаются с `status: "error"` и сообщением:

- `"company does not exist"` - компания не существует
- `"user does not exist"` - пользователь не существует
- `"route does not exist"` - маршрут не существует
- `"vehicle already exists"` - транспортное средство уже существует
- `"report not found or already verified"` - отчет не найден или уже верифицирован
- `"key is empty"` - пустой ключ в конфигурации
- `"company name is empty"` - пустое имя компании
- `"database error"` - внутренняя ошибка базы данных

---

## 📦 Скачивание базы данных

Для резервного копирования можно скачать всю базу данных компании в виде ZIP-архива:

```http
GET /db
```

---

## 🔄 Форматы данных

### Дата
Используйте формат `YYYY-MM-DD`

### Числа
- Целые числа для: `num_round_trips`, `num_passengers`
- Вещественные числа для: `salary`, `coef`

### Статусы отчетов
- `not_verified` - не проверен
- `verified` - проверен и одобрен
- `rejected` - отклонен

### Состояния транспортных средств
- `active` - активен
- `inactive` - неактивен
- `repair` - в ремонте

---

## 🏗️ Структура проекта для фронтенда

Рекомендуемая структура для фронтенд-приложения:

```
src/
├── api/
│   ├── company.js      # Работа с компаниями
│   ├── users.js        # Работа с пользователями
│   ├── routes.js       # Работа с маршрутами
│   ├── reports.js      # Работа с отчетами
│   ├── vehicles.js     # Работа с транспортом
│   ├── statistics.js   # Работа со статистикой
│   └── config.js       # Работа с конфигурацией
├── utils/
│   └── apiClient.js    # Обертка для fetch с обработкой ошибок
└── ...
```

---

## 📞 Поддержка

При возникновении проблем с API:
1. Проверьте наличие компании в списке
2. Убедитесь в правильности формата данных
3. Проверьте обязательные поля
4. Для сложных ошибок - обратитесь к backend-разработчику