# Документация по API для фронтенда

## Базовый URL
```
https://transdigital.pythonanywhere.com
```

## Общая информация

API предоставляет интерфейс для управления пользователями и транспортными средствами компаний. Все запросы, кроме GET, используют метод POST и принимают данные в формате JSON.

---

## 📁 Пользователи

### 1. Добавление пользователя
**Endpoint:** `POST /api/add_user/<company>`

**Параметры:**
- `company` (string) - название компании (например: "rotor")

**Тело запроса (JSON):**
```json
{
    "name": "Иван Иванов",
    "password": "password123",
    "post": "Менеджер",
    "account": "ivanov_i",
    "vk": "vk.com/ivanov",
    "disciplinary_actions": "0",
    "note": "Пример заметки"
}
```

**Примечание:** Поля `disciplinary_actions` и `note` являются необязательными. По умолчанию:
- `disciplinary_actions`: "0"
- `note`: "-"

**Ответ:**
```json
{
    "status": "ok",
    "message": "user added"
}
```
или
```json
{
    "status": "error",
    "message": "error description"
}
```

---

### 2. Обновление пользователя
**Endpoint:** `POST /api/update_user/<company>`

**Параметры:**
- `company` (string) - название компании

**Тело запроса (JSON):**
```json
{
    "name": "Иван Иванов",
    "password": "newpassword456",
    "post": "Старший менеджер",
    "account": "ivanov_i",
    "vk": "vk.com/ivanov_new",
    "disciplinary_actions": "1",
    "note": "Обновленная заметка"
}
```

**Примечание:** Поле `note` является необязательным. По умолчанию: "-"

**Ответ:**
```json
{
    "status": "ok", 
    "message": "user updated"
}
```
или
```json
{
    "status": "error",
    "message": "error description"
}
```

---

### 3. Удаление пользователя
**Endpoint:** `POST /api/delete_user/<company>`

**Параметры:**
- `company` (string) - название компании

**Тело запроса (JSON):**
```json
{
    "name": "Иван Иванов"
}
```

**Ответ:**
```json
{
    "status": "ok",
    "message": "user deleted"
}
```
или
```json
{
    "status": "error", 
    "message": "error description"
}
```

---

### 4. Получение базовой информации о пользователе
**Endpoint:** `POST /api/get_user/<company>`

**Параметры:**
- `company` (string) - название компании

**Тело запроса (JSON):**
```json
{
    "name": "Иван Иванов"
}
```

**Ответ:**
```json
{
    "status": "ok",
    "name": "Иван Иванов",
    "password": "password123"
}
```
или
```json
{
    "status": "error",
    "message": "error description"
}
```

---

### 5. Получение расширенной информации о пользователе
**Endpoint:** `POST /api/get_user_info/<company>`

**Параметры:**
- `company` (string) - название компании

**Тело запроса (JSON):**
```json
{
    "name": "Иван Иванов"
}
```

**Ответ:**
```json
{
    "status": "ok",
    "name": "Иван Иванов",
    "post": "Менеджер",
    "account": "ivanov_i",
    "vk": "vk.com/ivanov",
    "disciplinary_actions": "0",
    "note": "Пример заметки"
}
```
или
```json
{
    "status": "error",
    "message": "error description"
}
```

---

### 6. Получение информации о всех пользователях компании
**Endpoint:** `GET /api/get_users_info/<company>`

**Параметры:**
- `company` (string) - название компании

**Тело запроса:** не требуется

**Ответ:**
```json
{
    "status": "ok",
    "users": [
        {
            "name": "Иван Иванов",
            "post": "Менеджер",
            "account": "ivanov_i",
            "vk": "vk.com/ivanov",
            "disciplinary_actions": "0",
            "note": "Пример заметки"
        },
        {
            "name": "Петр Петров",
            "post": "Разработчик",
            "account": "petrov_p",
            "vk": "vk.com/petrov",
            "disciplinary_actions": "1",
            "note": "Другая заметка"
        }
    ]
}
```
или
```json
{
    "status": "error",
    "message": "error description"
}
```

---

## 🚗 Транспортные средства

### 7. Добавление транспортного средства
**Endpoint:** `POST /api/add_vehicle/<company>`

**Параметры:**
- `company` (string) - название компании

**Тело запроса (JSON):**
```json
{
    "board_number": "B-1234",
    "state_number": "A123BC777",
    "model": "Toyota Camry",
    "built": "2020",
    "since": "2021-01-15",
    "note": "Компанейский автомобиль",
    "state": "active",
    "owner": "Иван Иванов"
}
```

**Примечание:** Все поля, кроме `board_number`, являются необязательными и могут быть пустыми строками.

**Ответ:**
```json
{
    "status": "ok",
    "message": "vehicle added"
}
```
или
```json
{
    "status": "error",
    "message": "error description"
}
```

---

### 8. Получение всех транспортных средств компании
**Endpoint:** `GET /api/get_vehicles/<company>`

**Параметры:**
- `company` (string) - название компании

**Тело запроса:** не требуется

**Ответ:**
```json
{
    "status": "ok",
    "vehicles": [
        {
            "number": "1",
            "board_number": "B-1234",
            "state_number": "A123BC777",
            "model": "Toyota Camry",
            "built": "2020",
            "since": "2021-01-15",
            "note": "Компанейский автомобиль",
            "state": "active",
            "owner": "Иван Иванов"
        },
        {
            "number": "2",
            "board_number": "C-5678",
            "state_number": "B456DE999",
            "model": "Volkswagen Passat",
            "built": "2019",
            "since": "2020-03-20",
            "note": "Служебный транспорт",
            "state": "maintenance",
            "owner": "Петр Петров"
        }
    ]
}
```
или
```json
{
    "status": "error",
    "message": "error description"
}
```

---

### 9. Обновление транспортного средства
**Endpoint:** `POST /api/update_vehicle/<company>`

**Параметры:**
- `company` (string) - название компании

**Тело запроса (JSON):**
```json
{
    "number": "1",
    "board_number": "B-1234",
    "state_number": "A123BC777",
    "model": "Toyota Camry",
    "built": "2020",
    "since": "2021-01-15",
    "note": "Обновленная заметка",
    "state": "inactive",
    "owner": "Петр Петров"
}
```

**Примечание:** Все поля, кроме `number`, являются необязательными. Если поле не указано, оно останется без изменений.

**Ответ:**
```json
{
    "status": "ok",
    "message": "vehicle updated"
}
```
или
```json
{
    "status": "error",
    "message": "error description"
}
```

---

## 🚨 Коды ошибок

- `some fields are empty` - не все обязательные поля заполнены
- `company does not exist` - указанная компания не существует
- `user already exists` - пользователь с таким именем уже существует
- `user does not exist` - пользователь не найден
- `no users found` - в компании нет пользователей
- `board_number is empty` - не указан бортовой номер транспортного средства
- `vehicle already exists` - транспортное средство с таким бортовым номером уже существует
- `vehicle does not exist` - транспортное средство не найдено
- `no vehicles found` - в компании нет транспортных средств

---

## 💡 Пример использования (JavaScript)

```javascript
// Добавление пользователя
const addUser = async () => {
    const response = await fetch('https://transdigital.pythonanywhere.com/api/add_user/rotor', {
        method: 'POST',
        headers: {
            'Content-Type': 'application/json',
        },
        body: JSON.stringify({
            name: 'Иван Иванов',
            password: 'password123',
            post: 'Менеджер',
            account: 'ivanov_i',
            vk: 'vk.com/ivanov'
            // disciplinary_actions и note - необязательные
        })
    });
    const result = await response.json();
    console.log(result);
};

// Получение всех пользователей
const getUsers = async () => {
    const response = await fetch('https://transdigital.pythonanywhere.com/api/get_users_info/rotor', {
        method: 'GET'
    });
    const result = await response.json();
    console.log(result);
};

// Добавление транспортного средства
const addVehicle = async () => {
    const response = await fetch('https://transdigital.pythonanywhere.com/api/add_vehicle/rotor', {
        method: 'POST',
        headers: {
            'Content-Type': 'application/json',
        },
        body: JSON.stringify({
            board_number: 'B-1234',
            model: 'Toyota Camry',
            owner: 'Иван Иванов'
            // остальные поля - необязательные
        })
    });
    const result = await response.json();
    console.log(result);
};
```

---

## 📝 Примечания

- Все строковые поля обязательны для заполнения (кроме указанных как необязательные)
- Название компании в URL должно соответствовать зарегистрированным компаниям
- Для работы с API необходимо корректно обрабатывать CORS
- Все данные передаются в кодировке UTF-8
- `disciplinary_actions` по умолчанию "0", можно использовать для хранения количества дисциплинарных взысканий
- `note` по умолчанию "-", используется для дополнительных заметок
- `state` для транспортных средств может быть произвольным строковым значением (например: "active", "inactive", "maintenance")
- `owner` для транспортных средств - имя пользователя из таблицы пользователей