# 📚 Документация API для фронтенд-разработчика

## 🚀 Базовый URL

``` curl
https://transdigital.pythonanywhere.com
```

## 📋 Общая информация

API предоставляет RESTful интерфейс для управления системой учета транспорта, пользователей, маршрутов и отчетов. Все запросы, кроме GET, используют метод POST и принимают данные в формате JSON.

---

## 📊 Содержание

1. [🔗 Веб-интерфейс](#-веб-интерфейс)
2. [🏢 Управление компаниями](#-управление-компаниями)
3. [👥 Управление пользователями](#-управление-пользователями)
4. [🚗 Управление транспортными средствами](#-управление-транспортными-средствами)
5. [🗺️ Управление маршрутами](#️-управление-маршрутами)
6. [📊 Управление отчетами](#-управление-отчетами)
7. [⚠️ Коды ошибок](#️-коды-ошибок)
8. [💡 Примеры использования](#-примеры-использования-javascript)

---

## 🔗 Веб-интерфейс

### 1. Главная страница

**Эндпоинт:** `GET /`

**Описание:** Возвращает главную HTML страницу

---

### 2. Фавикон

**Эндпоинт:** `GET /favicon.ico`

**Описание:** Возвращает иконку сайта

---

## 🏢 Управление компаниями

### 1. Создать компанию

**Эндпоинт:** `POST /api/add_company`

**Тело запроса (JSON):**

```json
{
    "company": "название_компании"
}
```

**Ограничения:**

- Название компании не должно содержать пробелов
- Регистр не важен (приводится к нижнему)

**Пример ответа:**

```json
{
    "status": "ok",
    "message": "company added"
}
```

---

### 2. Удалить компанию

**Эндпоинт:** `POST /api/delete_company`

**Тело запроса (JSON):**

```json
{
    "company": "название_компании"
}
```

**Пример ответа:**

```json
{
    "status": "ok",
    "message": "company deleted"
}
```

---

## 👥 Управление пользователями

**Важно:** Все эндпоинты пользователей требуют указания названия компании в URL

### 1. Добавить пользователя

**Эндпоинт:** `POST /api/add_user/<company>`

**Тело запроса (JSON):**

```json
{
    "name": "Иван Иванов",
    "password": "пароль123",
    "post": "Водитель",
    "account": "ivanov_i",
    "vk": "vk.com/ivanov",
    "disciplinary_actions": "0",
    "note": "Отличный работник"
}
```

**Примечание:** Поля `disciplinary_actions` и `note` являются необязательными

**Пример ответа:**

```json
{
    "status": "ok",
    "message": "user added"
}
```

---

### 2. Обновить пользователя

**Эндпоинт:** `POST /api/update_user/<company>`

**Тело запроса (JSON):**

```json
{
    "id": "1",
    "name": "Иван Иванов",
    "password": "новыйпароль",
    "post": "Старший водитель",
    "account": "ivanov_i",
    "vk": "vk.com/ivanov_new",
    "disciplinary_actions": "1",
    "note": "Повышен"
}
```

**Примечание:** Все поля обязательны

**Пример ответа:**

```json
{
    "status": "ok",
    "message": "user updated"
}
```

---

### 3. Удалить пользователя

**Эндпоинт:** `POST /api/delete_user/<company>`

**Тело запроса (JSON):**

```json
{
    "id": "1"
}
```

**Пример ответа:**

```json
{
    "status": "ok",
    "message": "user deleted"
}
```

---

### 4. Получить пользователя

**Эндпоинт:** `POST /api/get_user/<company>`

**Тело запроса (JSON):**

```json
{
    "name": "Иван Иванов"
}
```

**ИЛИ**

```json
{
    "id": "1"
}
```

**Пример ответа:**

```json
{
    "status": "ok",
    "id": "1",
    "name": "Иван Иванов",
    "password": "пароль123",
    "post": "Водитель",
    "account": "ivanov_i",
    "vk": "vk.com/ivanov",
    "disciplinary_actions": "0",
    "note": "Отличный работник"
}
```

---

### 5. Получить всех пользователей

**Эндпоинт:** `GET /api/get_users/<company>`

**Пример ответа:**

```json
{
    "status": "ok",
    "users": [
        {
            "id": "1",
            "name": "Иван Иванов",
            "post": "Водитель",
            "account": "ivanov_i",
            "vk": "vk.com/ivanov",
            "disciplinary_actions": "0",
            "note": "Отличный работник"
        }
    ]
}
```

**Важно:** Пароль не возвращается в списке пользователей

---

## 🚗 Управление транспортными средствами

### 1. Добавить транспортное средство

**Эндпоинт:** `POST /api/add_vehicle/<company>`

**Тело запроса (JSON):**

```json
{
    "board_number": "А123БВ",
    "state_number": "А123БВ777",
    "model": "ГАЗель NEXT",
    "built": "2022",
    "since": "2023-01-15",
    "note": "Новый автомобиль",
    "state": "активен",
    "owner": "Иван Иванов"
}
```

**Обязательное поле:** `board_number`

**Пример ответа:**

```json
{
    "status": "ok",
    "message": "vehicle added"
}
```

---

### 2. Получить все транспортные средства

**Эндпоинт:** `GET /api/get_vehicles/<company>`

**Пример ответа:**

```json
{
    "status": "ok",
    "vehicles": [
        {
            "number": "1",
            "board_number": "А123БВ",
            "state_number": "А123БВ777",
            "model": "ГАЗель NEXT",
            "built": "2022",
            "since": "2023-01-15",
            "note": "Новый автомобиль",
            "state": "активен",
            "owner": "Иван Иванов"
        }
    ]
}
```

---

### 3. Обновить транспортное средство

**Эндпоинт:** `POST /api/update_vehicle/<company>`

**Тело запроса (JSON):**

```json
{
    "number": "1",
    "board_number": "А123БВ",
    "state": "на ремонте",
    "note": "Проходит плановое ТО"
}
```

**Примечание:** Все поля, кроме `number`, необязательны

**Пример ответа:**

```json
{
    "status": "ok",
    "message": "vehicle updated"
}
```

---

### 4. Удалить транспортное средство

**Эндпоинт:** `POST /api/delete_vehicle/<company>`

**Тело запроса (JSON):**

```json
{
    "number": "1"
}
```

**Пример ответа:**

```json
{
    "status": "ok",
    "message": "vehicle deleted"
}
```

---

## 🗺️ Управление маршрутами

### 1. Получить все маршруты

**Эндпоинт:** `GET /api/get_routes/<company>`

**Пример ответа:**

```json
{
    "status": "ok",
    "routes": ["Маршрут №1", "Маршрут №2", "Маршрут №3"]
}
```

---

### 2. Добавить маршрут

**Эндпоинт:** `POST /api/add_route/<company>`

**Тело запроса (JSON):**

```json
{
    "route": "Новый маршрут"
}
```

**Пример ответа:**

```json
{
    "status": "ok",
    "message": "route added"
}
```

---

### 3. Удалить маршрут

**Эндпоинт:** `POST /api/delete_route/<company>`

**Тело запроса (JSON):**

```json
{
    "route": "Маршрут №1"
}
```

**Пример ответа:**

```json
{
    "status": "ok",
    "message": "route deleted"
}
```

---

## 📊 Управление отчетами

### 1. Получить все отчеты

**Эндпоинт:** `GET /api/get_reports/<company>`

**Пример ответа:**

```json
{
    "status": "ok",
    "reports": [
        {
            "id": "1",
            "user_name": "Иван Иванов",
            "date": "2024-01-15",
            "route": "Маршрут №1",
            "num_round_trips": "5",
            "num_passengers": "45",
            "proof": "photo123.jpg",
            "status": "not verified"
        }
    ]
}
```

**Статусы отчетов:**

- `not verified` - не проверен
- `verified` - подтвержден
- `rejected` - отклонен

---

### 2. Добавить отчет

**Эндпоинт:** `POST /api/add_report/<company>`

**Тело запроса (JSON):**

```json
{
    "user_name": "Иван Иванов",
    "date": "2024-01-15",
    "route": "Маршрут №1",
    "num_round_trip": "5",
    "num_passengers": "45",
    "proof": "photo123.jpg"
}
```

**Все поля обязательны**

**Пример ответа:**

```json
{
    "status": "ok",
    "message": "report added"
}
```

---

### 3. Удалить отчет

**Эндпоинт:** `POST /api/delete_report/<company>`

**Тело запроса (JSON):**

```json
{
    "id": "1"
}
```

**Пример ответа:**

```json
{
    "status": "ok",
    "message": "report deleted"
}
```

---

### 4. Подтвердить отчет

**Эндпоинт:** `POST /api/verify_report/<company>`

**Тело запроса (JSON):**

```json
{
    "id": "1"
}
```

**Пример ответа:**

```json
{
    "status": "ok",
    "message": "report verified"
}
```

---

### 5. Отклонить отчет

**Эндпоинт:** `POST /api/reject_report/<company>`

**Тело запроса (JSON):**

```json
{
    "id": "1"
}
```

**Пример ответа:**

```json
{
    "status": "ok",
    "message": "report rejected"
}
```

---

## ⚠️ Коды ошибок

### Общие ошибки

- `company name is empty` - не указано название компании
- `company already exists` - компания уже существует
- `company does not exist` - компания не найдена
- `company name cannot contain spaces` - название компании содержит пробелы

### Ошибки пользователей

- `some fields are empty` - не все обязательные поля заполнены
- `user already exists` - пользователь уже существует
- `user does not exist` - пользователь не найден
- `no users found` - пользователи не найдены
- `name and id is empty` - не указаны ни имя, ни ID пользователя

### Ошибки транспортных средств

- `board_number is empty` - не указан бортовой номер
- `vehicle already exists` - транспортное средство уже существует
- `vehicle does not exist` - транспортное средство не найдено
- `no vehicles found` - транспортные средства не найдены

### Ошибки маршрутов

- `route is empty` - не указан маршрут
- `route already exists` - маршрут уже существует
- `route does not exist` - маршрут не найден
- `no routes found` - маршруты не найдены

### Ошибки отчетов

- `id is empty` - не указан ID
- `no reports found` - отчеты не найдены

---

## 💡 Примеры использования (JavaScript)

### Базовая настройка

```javascript
const BASE_URL = 'https://transdigital.pythonanywhere.com';
const COMPANY = 'rotor';

const request = async (endpoint, method = 'GET', data = null) => {
    const options = {
        method,
        headers: {
            'Content-Type': 'application/json',
        }
    };
    
    if (data) {
        options.body = JSON.stringify(data);
    }
    
    const response = await fetch(`${BASE_URL}${endpoint}`, options);
    return await response.json();
};
```

### Работа с пользователями

```javascript
// Добавить пользователя
const addUser = async () => {
    const result = await request(`/api/add_user/${COMPANY}`, 'POST', {
        name: 'Иван Иванов',
        password: 'пароль123',
        post: 'Водитель',
        account: 'ivanov_i',
        vk: 'vk.com/ivanov',
        disciplinary_actions: '0',
        note: 'Новый сотрудник'
    });
    console.log(result);
};

// Получить всех пользователей
const getUsers = async () => {
    const result = await request(`/api/get_users/${COMPANY}`);
    console.log(result);
};

// Обновить пользователя
const updateUser = async () => {
    const result = await request(`/api/update_user/${COMPANY}`, 'POST', {
        id: '1',
        name: 'Иван Иванов',
        password: 'новыйпароль',
        post: 'Старший водитель',
        account: 'ivanov_i',
        vk: 'vk.com/ivanov',
        disciplinary_actions: '1',
        note: 'Повышение'
    });
    console.log(result);
};
```

### Работа с отчетами

```javascript
// Добавить отчет
const addReport = async () => {
    const result = await request(`/api/add_report/${COMPANY}`, 'POST', {
        user_name: 'Иван Иванов',
        date: '2024-01-15',
        route: 'Маршрут №1',
        num_round_trip: '5',
        num_passengers: '45',
        proof: 'photo_20240115.jpg'
    });
    console.log(result);
};

// Подтвердить отчет
const verifyReport = async () => {
    const result = await request(`/api/verify_report/${COMPANY}`, 'POST', {
        id: '1'
    });
    console.log(result);
};
```

### Работа с транспортными средствами

```javascript
// Добавить транспортное средство
const addVehicle = async () => {
    const result = await request(`/api/add_vehicle/${COMPANY}`, 'POST', {
        board_number: 'А123БВ',
        model: 'ГАЗель NEXT',
        owner: 'Иван Иванов'
    });
    console.log(result);
};

// Получить все транспортные средства
const getVehicles = async () => {
    const result = await request(`/api/get_vehicles/${COMPANY}`);
    console.log(result);
};
```

### Работа с компаниями

```javascript
// Создать компанию
const createCompany = async () => {
    const result = await request('/api/add_company', 'POST', {
        company: 'newcompany'
    });
    console.log(result);
};
```

---

## 📝 Важные замечания

### 🔄 Совместимость

1. API поддерживает CORS
2. Все данные в кодировке UTF-8
3. Формат ответа всегда JSON

---

## 🆘 Поддержка

При возникновении проблем:

1. Проверьте наличие компании в системе
2. Убедитесь, что все обязательные поля заполнены
3. Проверьте формат передаваемых данных
4. Убедитесь, что название компании не содержит пробелов
5. Проверьте соединение с сервером
