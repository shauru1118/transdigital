# 📚 Документация API для фронтенд-разработчика

## 🚀 Базовый URL
```
https://transdigital.pythonanywhere.com
```

## 📋 Общая информация

API предоставляет RESTful интерфейс для управления многокомпанейской системой учета транспорта. Система включает управление пользователями, транспортными средствами, маршрутами, отчетами и статистикой. Все запросы, кроме GET, используют метод POST и принимают данные в формате JSON.

**Важное обновление:** Добавлены HTML-страницы для просмотра данных и новая система расчета зарплат с коэффициентами.

---

## 📊 Содержание

1. [🔗 Веб-интерфейс](#-веб-интерфейс)
2. [🏢 Управление компаниями](#-управление-компаниями)
3. [👥 Управление пользователями](#-управление-пользователями)
4. [🚗 Управление транспортными средствами](#-управление-транспортными-средствами)
5. [🗺️ Управление маршрутами](#-управление-маршрутами)
6. [📊 Управление отчетами](#-управление-отчетами)
7. [💰 Система зарплат и коэффициентов](#-система-зарплат-и-коэффициентов)
8. [⚙️ Настройки системы](#-настройки-системы)
9. [⚠️ Коды ошибок](#-коды-ошибок)
10. [💡 Примеры использования](#-примеры-использования)

---

## 🔗 Веб-интерфейс

### 1. Главная страница
**Эндпоинт:** `GET /`

**Описание:** Возвращает главную HTML страницу системы

---

### 2. Фавикон
**Эндпоинт:** `GET /favicon.ico`

**Описание:** Возвращает иконку сайта

---

### 3. Скачать базу данных
**Эндпоинт:** `GET /db`

**Описание:** Создает и возвращает ZIP-архив со всеми базами данных компаний

---

### 4. Дашборд пользователя
**Эндпоинт:** `GET /dashboard/<username>`

**Описание:** Возвращает HTML страницу личного кабинета пользователя

**Особенность:** Компания определяется автоматически из домена

**Пример:**
```
https://rotor.transdigital.pythonanywhere.com/dashboard/ivanov
```

---

### 5. Список пользователей компании
**Эндпоинт:** `GET /users`

**Описание:** Возвращает HTML страницу со списком всех пользователей компании

**Особенность:** Компания определяется автоматически из домена

**Пример:**
```
https://rotor.transdigital.pythonanywhere.com/users
```

---

## 🏢 Управление компаниями

### 1. Создать компанию
**Эндпоинт:** `POST /api/add_company`

**Тело запроса (JSON):**
```json
{
    "company": "название_компании"
}
```

**Ограничения:**
- Название компании не должно содержать пробелов
- Регистр не важен (приводится к нижнему)
- Используется для субдомена (company.transdigital.pythonanywhere.com)

**Пример ответа:**
```json
{
    "status": "ok",
    "message": "company added"
}
```

---

### 2. Удалить компанию
**Эндпоинт:** `POST /api/delete_company`

**Тело запроса (JSON):**
```json
{
    "company": "название_компании"
}
```

**Внимание:** Удаляет все данные компании безвозвратно!

**Пример ответа:**
```json
{
    "status": "ok",
    "message": "company deleted"
}
```

---

## 👥 Управление пользователями

**Важно:** Все эндпоинты пользователей требуют указания названия компании в URL

### 1. Добавить пользователя
**Эндпоинт:** `POST /api/add_user/<company>`

**Тело запроса (JSON):**
```json
{
    "name": "Иван Иванов",
    "password": "пароль123",
    "post": "Водитель",
    "account": "ivanov_i",
    "vk": "vk.com/ivanov",
    "disciplinary_actions": "0",
    "note": "Отличный работник"
}
```

**Примечание:** Поля `disciplinary_actions` и `note` являются необязательными

**Пример ответа:**
```json
{
    "status": "ok",
    "message": "user added"
}
```

---

### 2. Обновить пользователя
**Эндпоинт:** `POST /api/update_user/<company>`

**Тело запроса (JSON):**
```json
{
    "id": "1"
}
```

**Особенность:** Для обновления пользователя достаточно указать только ID. Система автоматически получит текущие данные пользователя и обновит только переданные поля.

**Полный пример обновления:**
```json
{
    "id": "1",
    "name": "Иван Иванов",
    "password": "новыйпароль",
    "post": "Старший водитель",
    "account": "ivanov_i",
    "vk": "vk.com/ivanov_new",
    "disciplinary_actions": "1",
    "note": "Повышен"
}
```

**Пример ответа:**
```json
{
    "status": "ok",
    "message": "user updated"
}
```

---

### 3. Удалить пользователя
**Эндпоинт:** `POST /api/delete_user/<company>`

**Тело запроса (JSON):**
```json
{
    "id": "1"
}
```

**Пример ответа:**
```json
{
    "status": "ok",
    "message": "user deleted"
}
```

---

### 4. Получить пользователя
**Эндпоинт:** `POST /api/get_user/<company>`

**Тело запроса (JSON):**
```json
{
    "name": "Иван Иванов"
}
```
**ИЛИ**
```json
{
    "id": "1"
}
```

**Пример ответа:**
```json
{
    "status": "ok",
    "id": "1",
    "name": "Иван Иванов",
    "password": "пароль123",
    "post": "Водитель",
    "account": "ivanov_i",
    "vk": "vk.com/ivanov",
    "disciplinary_actions": "0",
    "note": "Отличный работник"
}
```

---

### 5. Получить всех пользователей
**Эндпоинт:** `GET /api/get_users/<company>`

**Пример ответа:**
```json
{
    "status": "ok",
    "users": [
        {
            "id": "1",
            "name": "Иван Иванов",
            "post": "Водитель",
            "account": "ivanov_i",
            "vk": "vk.com/ivanov",
            "disciplinary_actions": "0",
            "note": "Отличный работник"
        },
        {
            "id": "2",
            "name": "Петр Петров",
            "post": "Менеджер",
            "account": "petrov_p",
            "vk": "vk.com/petrov",
            "disciplinary_actions": "2",
            "note": "Новый сотрудник"
        }
    ]
}
```

**Важно:** Пароль не возвращается в списке пользователей

---

## 🚗 Управление транспортными средствами

### 1. Добавить транспортное средство
**Эндпоинт:** `POST /api/add_vehicle/<company>`

**Тело запроса (JSON):**
```json
{
    "board_number": "А123БВ",
    "state_number": "А123БВ777",
    "model": "ГАЗель NEXT",
    "built": "2022",
    "since": "2023-01-15",
    "note": "Новый автомобиль",
    "state": "активен",
    "owner": "Иван Иванов"
}
```

**Обязательное поле:** `board_number`

**Пример ответа:**
```json
{
    "status": "ok",
    "message": "vehicle added"
}
```

---

### 2. Получить все транспортные средства
**Эндпоинт:** `GET /api/get_vehicles/<company>`

**Пример ответа:**
```json
{
    "status": "ok",
    "vehicles": [
        {
            "number": "1",
            "board_number": "А123БВ",
            "state_number": "А123БВ777",
            "model": "ГАЗель NEXT",
            "built": "2022",
            "since": "2023-01-15",
            "note": "Новый автомобиль",
            "state": "активен",
            "owner": "Иван Иванов"
        }
    ]
}
```

---

### 3. Обновить транспортное средство
**Эндпоинт:** `POST /api/update_vehicle/<company>`

**Тело запроса (JSON):**
```json
{
    "number": "1",
    "board_number": "А123БВ",
    "state": "на ремонте",
    "note": "Проходит плановое ТО"
}
```

**Примечание:** Все поля, кроме `number`, необязательны. Если поле не указано, оно останется без изменений.

**Пример ответа:**
```json
{
    "status": "ok",
    "message": "vehicle updated"
}
```

---

### 4. Удалить транспортное средство
**Эндпоинт:** `POST /api/delete_vehicle/<company>`

**Тело запроса (JSON):**
```json
{
    "number": "1"
}
```

**Пример ответа:**
```json
{
    "status": "ok",
    "message": "vehicle deleted"
}
```

---

## 🗺️ Управление маршрутами

### 1. Получить все маршруты
**Эндпоинт:** `GET /api/get_routes/<company>`

**Пример ответа:**
```json
{
    "status": "ok",
    "routes": [
        {
            "id": 1,
            "route": "Маршрут №1",
            "salary": "5000"
        },
        {
            "id": 2,
            "route": "Маршрут №2", 
            "salary": "7500"
        }
    ]
}
```

**Примечание:** Каждый маршрут теперь имеет фиксированную стоимость (salary)

---

### 2. Добавить маршрут
**Эндпоинт:** `POST /api/add_route/<company>`

**Тело запроса (JSON):**
```json
{
    "route": "Новый маршрут",
    "salary": "6000"
}
```

**Обязательные поля:** `route`, `salary`

**Пример ответа:**
```json
{
    "status": "ok",
    "message": "route added"
}
```

---

### 3. Удалить маршрут
**Эндпоинт:** `POST /api/delete_route/<company>`

**Тело запроса (JSON):**
```json
{
    "id": "1"
}
```

**Важно:** Теперь требуется передавать ID маршрута, а не название

**Пример ответа:**
```json
{
    "status": "ok",
    "message": "route deleted"
}
```

---

## 📊 Управление отчетами

### 1. Получить все отчеты
**Эндпоинт:** `GET /api/get_reports/<company>`

**Пример ответа:**
```json
{
    "status": "ok",
    "reports": [
        {
            "id": "1",
            "user_name": "Иван Иванов",
            "date": "2024-01-15",
            "route": "Маршрут №1",
            "num_round_trips": "5",
            "num_passengers": "45",
            "proof": "photo123.jpg",
            "status": "not verified"
        }
    ]
}
```

**Статусы отчетов:**
- `not verified` - не проверен
- `verified` - подтвержден (автоматически начисляет зарплату)
- `rejected` - отклонен

---

### 2. Добавить отчет
**Эндпоинт:** `POST /api/add_report/<company>`

**Тело запроса (JSON):**
```json
{
    "user_name": "Иван Иванов",
    "date": "2024-01-15",
    "route": "Маршрут №1",
    "num_round_trip": "5",
    "num_passengers": "45",
    "proof": "photo123.jpg"
}
```

**Все поля обязательны**

**Пример ответа:**
```json
{
    "status": "ok",
    "message": "report added"
}
```

---

### 3. Удалить отчет
**Эндпоинт:** `POST /api/delete_report/<company>`

**Тело запроса (JSON):**
```json
{
    "id": "1"
}
```

**Пример ответа:**
```json
{
    "status": "ok",
    "message": "report deleted"
}
```

---

### 4. Подтвердить отчет
**Эндпоинт:** `POST /api/verify_report/<company>`

**Тело запроса (JSON):**
```json
{
    "id": "1"
}
```

**Что происходит при подтверждении:**
1. Рассчитывается зарплата по формуле: `(рейсы × стоимость_маршрута + пассажиры × стоимость_пассажира) × коэффициент_должности`
2. Обновляется статистика пользователя
3. Статус отчета меняется на "verified"

**Пример ответа:**
```json
{
    "status": "ok",
    "message": "report verified"
}
```

---

### 5. Отклонить отчет
**Эндпоинт:** `POST /api/reject_report/<company>`

**Тело запроса (JSON):**
```json
{
    "id": "1"
}
```

**Пример ответа:**
```json
{
    "status": "ok",
    "message": "report rejected"
}
```

---

## 💰 Система зарплат и коэффициентов

### Формула расчета зарплаты:
```
Зарплата = (рейсы × стоимость_маршрута + пассажиры × стоимость_пассажира) × коэффициент_должности
```

### 1. Получить все коэффициенты должностей
**Эндпоинт:** `GET /api/get_coefs/<company>`

**Пример ответа:**
```json
{
    "status": "ok",
    "coefs": [
        {
            "id": "1",
            "post": "Водитель",
            "coef": "1.0"
        },
        {
            "id": "2",
            "post": "Старший водитель",
            "coef": "1.2"
        }
    ]
}
```

---

### 2. Добавить коэффициент
**Эндпоинт:** `POST /api/add_coef/<company>`

**Тело запроса (JSON):**
```json
{
    "post": "Менеджер",
    "coef": "1.5"
}
```

**Пример ответа:**
```json
{
    "status": "ok",
    "message": "coef added"
}
```

---

### 3. Обновить коэффициент
**Эндпоинт:** `POST /api/update_coef/<company>`

**Тело запроса (JSON):**
```json
{
    "id": "1",
    "post": "Водитель",
    "coef": "1.1"
}
```

**Пример ответа:**
```json
{
    "status": "ok",
    "message": "coef updated"
}
```

---

### 4. Удалить коэффициент
**Эндпоинт:** `POST /api/delete_coef/<company>`

**Тело запроса (JSON):**
```json
{
    "id": "1"
}
```

**Пример ответа:**
```json
{
    "status": "ok",
    "message": "coef deleted"
}
```

---

### 5. Получить статистику пользователя
**Эндпоинт:** `POST /api/get_user_statistics/<company>`

**Тело запроса (JSON):**
```json
{
    "user_name": "Иван Иванов"
}
```

**Пример ответа:**
```json
{
    "status": "ok",
    "id": "1",
    "user_name": "Иван Иванов",
    "total_salary": "125000",
    "period_salary": "25000",
    "total_round_trips": "50",
    "period_round_trips": "10",
    "total_passengers": "450",
    "period_passengers": "90"
}
```

**Поля статистики:**
- `total_salary` - общая зарплата за все время
- `period_salary` - зарплата за текущий период
- `total_round_trips` - общее количество рейсов
- `period_round_trips` - рейсы за текущий период
- `total_passengers` - общее количество пассажиров
- `period_passengers` - пассажиры за текущий период

---

## ⚙️ Настройки системы

### 1. Получить все настройки
**Эндпоинт:** `GET /api/get_configs/<company>`

**Пример ответа:**
```json
{
    "status": "ok",
    "configs": [
        {
            "id": "1",
            "key": "passanger_cost",
            "value": "2000"
        }
    ]
}
```

---

### 2. Изменить настройку
**Эндпоинт:** `POST /api/set_config/<company>`

**Тело запроса (JSON):**
```json
{
    "key": "passanger_cost",
    "value": "2500"
}
```

**Пример ответа:**
```json
{
    "status": "ok",
    "message": "config updated"
}
```

---

### 3. Добавить настройку
**Эндпоинт:** `POST /api/add_config/<company>`

**Тело запроса (JSON):**
```json
{
    "key": "min_salary",
    "value": "50000"
}
```

**Пример ответа:**
```json
{
    "status": "ok",
    "message": "config added"
}
```

---

### 4. Удалить настройку
**Эндпоинт:** `POST /api/delete_config/<company>`

**Тело запроса (JSON):**
```json
{
    "key": "min_salary"
}
```

**Пример ответа:**
```json
{
    "status": "ok",
    "message": "config deleted"
}
```

---

## ⚠️ Коды ошибок

### Общие ошибки
- `company name is empty` - не указано название компании
- `company already exists` - компания уже существует
- `company does not exist` - компания не найдена
- `company name cannot contain spaces` - название компании содержит пробелы

### Ошибки пользователей
- `some fields are empty` - не все обязательные поля заполнены
- `user already exists` - пользователь уже существует
- `user does not exist` - пользователь не найден
- `no users found` - пользователи не найдены
- `name and id is empty` - не указаны ни имя, ни ID пользователя
- `id is empty` - не указан ID пользователя

### Ошибки транспортных средств
- `board_number is empty` - не указан бортовой номер
- `vehicle already exists` - транспортное средство уже существует
- `vehicle does not exist` - транспортное средство не найдено
- `no vehicles found` - транспортные средства не найдены

### Ошибки маршрутов
- `route is empty` - не указан маршрут
- `route already exists` - маршрут уже существует
- `route does not exist` - маршрут не найден
- `no routes found` - маршруты не найдены

### Ошибки отчетов
- `id is empty` - не указан ID
- `no reports found` - отчеты не найдены
- `some fields are empty` - не все обязательные поля заполнены

### Ошибки коэффициентов и статистики
- `no coefs found` - коэффициенты не найдены
- `no coef found` - коэффициент для данной должности не найден
- `users statistics does not exist` - статистика пользователя не найдена

---

## 💡 Примеры использования (JavaScript)

### Базовая настройка
```javascript
const BASE_URL = 'https://transdigital.pythonanywhere.com';
const COMPANY = 'rotor';

const request = async (endpoint, method = 'GET', data = null) => {
    const options = {
        method,
        headers: {
            'Content-Type': 'application/json',
        }
    };
    
    if (data) {
        options.body = JSON.stringify(data);
    }
    
    const response = await fetch(`${BASE_URL}${endpoint}`, options);
    return await response.json();
};
```

### Работа с пользователями
```javascript
// Добавить пользователя
const addUser = async () => {
    const result = await request(`/api/add_user/${COMPANY}`, 'POST', {
        name: 'Иван Иванов',
        password: 'пароль123',
        post: 'Водитель',
        account: 'ivanov_i',
        vk: 'vk.com/ivanov',
        disciplinary_actions: '0',
        note: 'Новый сотрудник'
    });
    console.log(result);
};

// Частичное обновление пользователя
const updateUserPartial = async () => {
    // Обновляем только пароль
    const result = await request(`/api/update_user/${COMPANY}`, 'POST', {
        id: '1',
        password: 'новый_пароль'
    });
    console.log(result);
};
```

### Работа с отчетами и зарплатами
```javascript
// Добавить отчет
const addReport = async () => {
    const result = await request(`/api/add_report/${COMPANY}`, 'POST', {
        user_name: 'Иван Иванов',
        date: '2024-01-15',
        route: 'Маршрут №1',
        num_round_trip: '5',
        num_passengers: '45',
        proof: 'photo_20240115.jpg'
    });
    console.log(result);
};

// Подтвердить отчет (начислить зарплату)
const verifyReport = async () => {
    const result = await request(`/api/verify_report/${COMPANY}`, 'POST', {
        id: '1'
    });
    console.log(result);
};

// Получить статистику пользователя
const getUserStats = async () => {
    const result = await request(`/api/get_user_statistics/${COMPANY}`, 'POST', {
        user_name: 'Иван Иванов'
    });
    console.log(result);
};
```

### Работа с коэффициентами
```javascript
// Добавить коэффициент для должности
const addCoefficient = async () => {
    const result = await request(`/api/add_coef/${COMPANY}`, 'POST', {
        post: 'Механик',
        coef: '1.3'
    });
    console.log(result);
};

// Получить все коэффициенты
const getCoefficients = async () => {
    const result = await request(`/api/get_coefs/${COMPANY}`);
    console.log(result);
};
```

### Работа с настройками
```javascript
// Изменить стоимость пассажира
const updatePassengerCost = async () => {
    const result = await request(`/api/set_config/${COMPANY}`, 'POST', {
        key: 'passanger_cost',
        value: '2500'
    });
    console.log(result);
};
```

### Использование HTML-страниц
```javascript
// Переход в личный кабинет
const openDashboard = (username) => {
    window.location.href = `https://${COMPANY}.transdigital.pythonanywhere.com/dashboard/${username}`;
};

// Просмотр списка пользователей
const openUsersList = () => {
    window.location.href = `https://${COMPANY}.transdigital.pythonanywhere.com/users`;
};
```

---

## 📝 Важные замечания

### 🔄 Новая система расчета зарплат
1. **Формула расчета:** `(рейсы × стоимость_маршрута + пассажиры × стоимость_пассажира) × коэффициент_должности`
2. **Стоимость пассажира:** Настраивается в конфигурации (по умолчанию 2000)
3. **Стоимость маршрута:** Устанавливается при создании маршрута
4. **Коэффициенты:** Зависят от должности пользователя

### 🌐 Мультидоменная архитектура
1. Каждая компания доступна по своему субдомену: `company.transdigital.pythonanywhere.com`
2. HTML-страницы автоматически определяют компанию из домена
3. API запросы требуют явного указания компании

### 🔒 Безопасность
1. Пароли хранятся в базе данных в открытом виде (необходима дополнительная защита)
2. Реализуйте авторизацию на фронтенде
3. Используйте HTTPS для всех запросов

### 📊 Статистика
1. Ведутся два типа статистики: общая и за период
2. При подтверждении отчета статистика обновляется автоматически
3. Период можно сбрасывать через административный интерфейс

### ⚡ Производительность
1. Используйте кэширование для статических данных
2. Загружайте списки с пагинацией при большом объеме данных
3. Обрабатывайте ошибки сети и таймауты

### 🔄 Совместимость
1. API поддерживает CORS
2. Все данные в кодировке UTF-8
3. Формат ответа всегда JSON

---

## 🆘 Поддержка

При возникновении проблем:

1. **Проверьте наличие компании:** `company.transdigital.pythonanywhere.com`
2. **Убедитесь в правильности данных:** Все обязательные поля должны быть заполнены
3. **Проверьте формат данных:** Даты в формате "YYYY-MM-DD", числа как строки
4. **Убедитесь в отсутствии пробелов:** Название компании без пробелов
5. **Проверьте соединение:** Убедитесь, что сервер доступен

**Для скачивания резервной копии всех баз данных:**
```
https://transdigital.pythonanywhere.com/db
```

**Для доступа к HTML-интерфейсу:**
```
https://company.transdigital.pythonanywhere.com/dashboard/username
https://company.transdigital.pythonanywhere.com/users
```

---

## 🎯 Лучшие практики

1. **Храните ID:** Сохраняйте ID пользователей, транспортных средств и отчетов на фронтенде
2. **Валидация данных:** Проверяйте данные перед отправкой на сервер
3. **Обработка ошибок:** Реализуйте обработку всех возможных ошибок API
4. **Загрузка состояния:** Показывайте индикаторы загрузки при выполнении запросов
5. **Обновление данных:** Реализуйте периодическое обновление данных или обновление по действию пользователя

---

**Версия API:** 1.0.4  
**Дата последнего обновления:** Январь 2026  
**Поддержка:** multi-company, статистика, зарплаты, HTML-интерфейс